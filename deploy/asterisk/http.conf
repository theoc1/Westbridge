; Deliberately disabled.
;
; Westbridge needs raw AMI on TCP/5038 and nothing else. The built-in HTTP
; server would only be required for AMI-over-HTTP (/rawman, /mxml) or ARI, and
; neither can see a dialplan-created ConfBridge conference anyway.
[general]
enabled = no
